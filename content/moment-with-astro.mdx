---
status: "published"
title: "使用 Astro 打造速度飛快的靜態網站"
description: "在蓋「Moment：看電影看劇時，聽到喜歡的音樂」專案的時候，覺得輕量級的網站，用 Markdown 語法可以滿足快速、便於更新內容的需求。Astro 還未進入穩定版本。這篇文章是文件上找不到參考，只好到處從社群爬文記下來的做法。"
datePublished: "2022-02-16"
dateModified: "2022-02-16"
meta:
  - name: og:type
    content: "article"
  - name: article:published_time
    content: 2022-02-16T15:12:00
  - name: article:author
    content: "ymcheung"
slug: "moment-with-astro"
---

import { Article } from "~/components/article";

<Article>

# {frontmatter.title}

在蓋「[Moment：看電影看劇時，聽到喜歡的音樂](https://moment.intersection.tw)」專案的時候，覺得輕量級的網站，用 Markdown 語法可以滿足快速、便於更新內容的需求，找到了 [Remix](https://remix.run) 和 Astro。這個部落格使用 Remix；在 Moment 使用 Astro。

Astro 還未進入穩定版本。這篇文章不會再講一次 Astro 文件提到的功能，而是文件上找不到參考，只好到處從社群爬文記下來的做法。

## ![Astro 的標誌](/images/moment-with-astro/sticker.svg)

```json
"astro": "^0.22.20"
```

- 網站：https://astro.build
- 文件：https://docs.astro.build/zh-tw/getting-started/

Moment 專案根據 Astro 的 [Blog Starter 範例](https://github.com/withastro/astro/tree/main/examples/blog)進行修改，基礎是 `preact`，使用的體感跟 React 幾乎沒有差別，只有 JSX 的 `dangerouslySetInnerHtml` 無法使用。

## Astro v.s. Remix

Remix 的開發體驗確實很棒，社群也非常活絡。但畢竟是全端套件，如果只是要做靜態網站，以後會優先選擇 Astro。

### Astro

- 可以發布到靜態網站服務：Github Pages、Vercel 或 Netlify 都行，也就是可以免費。
- 官方不建議使用 `CSS-In-JS`，而是有 SCSS（或者更像 CSS Modules）和 PostCSS 支援。

### Remix

- 可以使用 `CSS-In-JS`，已經有人做好 [Stitches Starter](https://github.com/jjenzz/remix-stitches)。
- 雖然同樣可以做成靜態網頁，但 MDX 放在路徑資料夾裡的時候，連路徑名稱都無法取得，目前實用程度不高。
- 若要使用 `mdx-bundler`，無法在靜態網站服務使用，得用 [fly.io](https://fly.io) 這樣的服務，有一些些費用。

## 加入結構化資料 JSON LD 語法

按照直覺把結構化資料放在 `<head> ... </head>` 裡面時，會發現無法如預期輸出 JSON LD 語法：

```jsx
---
const schema = JSON.stringify({
  '@context': 'https://schema.org',
  '@graph': [{
    '@type': 'WebPage',
    url: `${import.meta.env.PUBLIC_HOSTNAME}${canonicalPath}`,
    name: title,
    datePublished: published,
    dateModified: modified
  }]
});
---

<script type="application/ld+json">{schema}</script>
```

`<script>` 的部分得要用 Template Literals 才會產生預期的 JSON LD 語法：

```jsx
{`<script type="application/ld+json">${schema}</script>`}
```

</Article>
